source .venv/bin/activate
# Workaround: Python 3.14 skips .pth files with macOS UF_HIDDEN flag (files
# starting with "__"), so the editable install path doesn't get added.
export PYTHONPATH="$PWD/src${PYTHONPATH:+:$PYTHONPATH}"
